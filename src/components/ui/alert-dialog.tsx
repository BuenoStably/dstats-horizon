import { Dialog, DialogTitle, DialogContent, DialogActions, Button } from '@mui/material';

export const AlertDialog = Dialog;
export const AlertDialogTrigger = Button;
export const AlertDialogContent = DialogContent;
export const AlertDialogHeader = DialogTitle;
export const AlertDialogFooter = DialogActions;
export const AlertDialogTitle = DialogTitle;
export const AlertDialogDescription = DialogContent;
export const AlertDialogAction = Button;
export const AlertDialogCancel = Button;